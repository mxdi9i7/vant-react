export interface IProps {
  name?: string;
  disabled?: boolean;
  checked: boolean;
  labelDisabled?: boolean;
  rtl?: boolean;
  iconSize?: string;
  checkedColor?: string;
  onClick: Function;
  label?: string;
}
